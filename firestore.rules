rules_version = '2';

service cloud.firestore {

  match /databases/{database}/documents {

    // Allow all reads and writes for all collections and documents

    match /{document=**} {

      allow read, write: if true;  // This allows any read or write operation by anyone

    }

  }

}